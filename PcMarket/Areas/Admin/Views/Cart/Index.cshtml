@model PcMarket.ViewModels.CartViewModel.CartViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
@if (Model.CartItems.Count > 0)
{
    <div>
        <h4>CartViewModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CartItems)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CartItems)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.GrandTotal)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.GrandTotal)
            </dd>
        </dl>
    </div>
    <div>
        @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        <a asp-action="Index">Back to List</a>
    </div>
}
else
{
    <div class="display-4 text-center">Your cart is empty</div>
}
